workflow: GitHubFlow/v1

branches:
  main:
    label: alpha
    increment: Minor
    tracks-release-branches: true
  release:
    prevent-increment:
      when-current-commit-tagged: true # Use tag value when tagged on release branch
